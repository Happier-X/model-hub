# v0.0.9 (2026-08-03)

## 修复

- 悬浮条在相同 DPI 下切屏后正确跟随，修复窗口布局异常（#3）。

## 功能

- 分组页改为响应式卡片网格布局：窄屏单列、中屏双列、宽屏三列，每张卡片展示分组摘要、模型队列与操作入口。
- 用 happier-ui 组件替换手写 UI：更新通知栏关闭按钮改用 `HButton`，更新下载进度改用 `HProgress` 进度条。

## 安装与更新

- 安装包：GitHub Release 中的 NSIS `.exe`
- 校验：同 Release 的 `SHA256SUMS.txt`
- 应用内更新清单：`https://github.com/Happier-X/model-hub/releases/latest/download/latest.json`