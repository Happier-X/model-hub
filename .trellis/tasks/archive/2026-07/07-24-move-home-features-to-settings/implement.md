# 首页功能拆分到设置 - 实现计划

## 1. 准备与基线

- [x] 读取前端目录、组件、状态、类型与质量规范。
- [x] 搜索首页端口、数据目录、更新偏好和「进入首页时自动检查」的全部引用。
- [x] 记录基线验证结果。

## 2. 新增设置页

- [x] 创建 `src/pages/SettingsPage.vue`。
- [x] 迁移端口输入、保存端口、数据目录读取和相关加载/错误/成功状态。
- [x] 迁移应用更新检查、版本展示、下载安装、取消和资源释放逻辑。
- [x] 迁移自动检查偏好读取与保存，更新展示文案为应用启动语义。
- [x] 保证浏览器环境下更新功能仍给出桌面应用提示。

## 3. 精简首页

- [x] 从 `HomePage.vue` 删除端口修改、数据目录、更新面板和自动检查偏好。
- [x] 删除不再使用的 import、ref、computed、方法与生命周期逻辑。
- [x] 保留代理状态、Base URL、监听地址、启停/刷新、统计和接入指引。
- [x] 确认首页代理刷新不再读取设置偏好或数据目录。

## 4. 应用启动自动检查

- [x] 在应用壳层实现启动后读取偏好并静默检查一次。
- [x] 发现新版本时在壳顶部显示可关闭提示，带「前往设置」跳转 `/settings`。
- [x] 确认检查失败不阻塞渲染、无更新不提示、路由切换不重复检查。
- [x] 确认不会自动下载安装或泄漏 `Update` 资源（探测后立即释放）。

## 5. 路由与导航

- [x] 添加 `/settings` 路由、`settings` name 和「设置」标题。
- [x] 侧边栏添加设置入口。
- [x] 验证 `/settings` 直接加载和导航激活态。

## 6. 文档与规范

- [x] 更新 README 和 docs 中端口、数据目录、更新入口的页面称呼。
- [x] 更新 `.trellis/spec/frontend/` 信息架构、目录结构、页面职责和自动检查语义。
- [x] 搜索旧的「进入首页时自动检查更新」及错误页面职责描述，确认无残留。

## 7. 验证

- [x] `npm run typecheck`
- [x] `npm run lint`
- [x] `npm run test:unit`
- [x] 检查 git diff，确保无后端协议或配置字段意外变化。
- [x] 独立 `trellis-check` 复核 PRD 和页面数据流。

## 回滚点

- 若设置页更新资源生命周期出现问题，保留页面拆分但先将更新面板恢复到首页，避免提交不可安装更新的状态。
- 若应用壳层静默检查缺少合适提示承载，优先保证「仅启动检查一次且不阻塞」，不引入新的全局通知系统。
