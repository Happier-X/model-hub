# 执行计划：真上游 Chat 上手与闭环自检

## 清单

1. [x] 扩展 `gatewayHttp`：`authMode` + `clientProbe`（永不附带 admin JWT）
2. [x] `DashboardPage`：客户端路径自检（models + 可选 chat）
3. [x] `docs/chat-onboarding.md` + README / client-integration / mvp-acceptance 互链
4. [x] `pnpm lint` / `pnpm build` / smoke
5. [x] 勾选 AC
